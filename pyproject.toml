[project]
name = "mlops-dvc-dataversioncontrol"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dvc>=3.67.1",
    "pandas>=3.0.6",
]

[project.scripts]
mlops-dvc-dataversioncontrol = "mlops_dvc_dataversioncontrol:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
